[project]
name = "akita-in-jolt-benchmark"
version = "0.1.0"
description = "Reproducible end-to-end Jolt benchmark for Akita and Dory"
requires-python = ">=3.11"

[project.scripts]
jolt-pcs-bench = "jolt_pcs_bench:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
